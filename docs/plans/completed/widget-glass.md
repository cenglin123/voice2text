---
status: done
mode: direct-execution
coordinator: ""
created_at: 2026-09-18
---

# 悬浮窗比例与原生毛玻璃

用户已授权调整悬浮窗圆角、左上角平衡并实现毛玻璃。

## 设计与边界

- 深蓝底 #18263E、冷灰边 #526482、正文 #F4F7FC、次级 #AAB8CC、聆听珊瑚红 #FF635F。
- 继续使用微软雅黑，标题与右侧操作同一水平线；340×104 基础尺寸，圆角约 22 像素，麦克风缩小并下移，保留状态与声波位置。
- 在本机 Windows 10 实测系统合成模糊；保留纯绘制回退，不增加 GUI 框架、不截图桌面作为运行期背景、不改录音链路。
- 对比 assets 及用户截图：控制左右曲率与视觉重心，不增加导航或无关装饰。

## 执行与验收

1. 独立预览进程验证原生模糊与透明绘制兼容性，再确定实现。
2. 调整圆角、字体锚点、图标、声波；验证五状态与 50%/100%/150% 缩放。
3. 验证模糊开关、缩放、拖动、显隐、回退与退出资源释放；对高对比测试背景截图，确认是真模糊。
4. 独立视角复核原生句柄与回退，更新记录、运行项目检查并提交。

## 验收结果

- 本机 Windows 10 19045 的细条纹与文字测试背景被系统实时模糊，圆角外无矩形底；待命、聆听截图通过。
- `python scripts/check_widget.py` 通过五状态、三尺寸、命中区、连续拖动与透明度、显隐、GDI 稳定、ULW 注入失败回退、不支持模糊的回退。
- 用户追加托盘图标要求：麦克风主体加宽、支架加粗、四状态超采样；16/20/24/32px 检查通过。
- 独立本地 reviewer 提出的 HWND 签名建议已修正；拖动事件坐标与 HANDLE 返回类型两项疑问经运行验证排除。外部评审被自动审批阻止，未发送源码。
- 兼容边界：Windows 11 未实机验证；系统效果设置在初始化时检查。没有改变录音、识别、校对链路。
